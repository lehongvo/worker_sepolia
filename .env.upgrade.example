# Example configurations for upgrading

# Example 1: Update both name and symbol
TOKEN_NAME_V2=MyNewToken
TOKEN_SYMBOL_V2=MNT

# Example 2: Update only symbol, keep name
# TOKEN_NAME_V2=              # Leave empty or comment out
TOKEN_SYMBOL_V2=NEWMTK

# Example 3: Update only name, keep symbol
TOKEN_NAME_V2=UpdatedToken
# TOKEN_SYMBOL_V2=            # Leave empty or comment out

# Example 4: Keep both (no changes)
# TOKEN_NAME_V2=              # Leave both empty
# TOKEN_SYMBOL_V2=

# The script will use current values from deployments/network.json if V2 vars are empty
